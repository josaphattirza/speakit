import { StyleSheet, View, Text } from "react-native";

export default function Listening() {
    return (
        <View>
            <Text>Listening Page</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    container: {
      flex: 1,
      padding: 20,
    },
  });